- with_cache('definitions', act) do
  #definitions-container(style='display: none')
    - for term_id, pair in act.term_definitions
      - term, defn = pair
      %div{id: term_id, data: {term: term}}
        = fragment_to_html(defn)
